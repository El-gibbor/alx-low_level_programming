#include <stdio.h>

/**
 * mod - function that finds the remainder of two values
 * @a: paremeter to check
 * @b: paremeter to check
 * Return: 0 (succes)
 */
int mod(int a, int b)
{
	return (a % b);
}
