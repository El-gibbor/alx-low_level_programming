#include <stdlib.h>
#include "main.h"

/**
 * strtow - memory allocation & splits a string into words
 * @str: string to be splitted
 * Return: a pointer to an array of strings.
*/
char **strtow(char *str)
{
    if (!str || str == "")
        return (NULL);
        

}